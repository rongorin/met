 
    @if (Model == null)
    { 
            <input type="submit" value="Delete" class="btn btn-danger" />   
   
    }
    else if (Model.Entity_ID == ViewBag.EntityId)
    { 
            <input type="submit" value="Delete" class="btn btn-danger" />  
       
    }

 